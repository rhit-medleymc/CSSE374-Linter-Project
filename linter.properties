# Linter configuration
# Set to "all" to enable every registered linter, or list class names separated by commas.
enabled_linters=SRPLinter

# Thresholds
# Flag methods/constructors with parameter count greater than this value.
too_many_parameters_limit=3

# Flag classes with LCOM >= this value for SRP checks.
srp_lcom_threshold=2
